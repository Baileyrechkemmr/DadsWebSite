{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput --ignore=*.pyc && gunicorn omimi.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 2 --max-requests 1000 --max-requests-jitter 100",
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}